@startuml

left to right direction

actor Student
actor Car
actor Database

rectangle "Web Application" {
  usecase "Send Commands" as send
  usecase "View Dashboard" as viewdash
  usecase "Filter Data" as filter
  usecase "Export Data" as export
  usecase "View Instructions" as viewinst
  usecase "Receive Commands" as receive
  usecase "Remove Commands" as remove
  usecase "Move Car" as move
  usecase "Retrieve Data" as retrieve
  usecase "Create/Save Data" as save

  (viewdash) <. (export) : <<extends>>
  (filter) .> (viewdash) : <<extends>>
  (retrieve) <. (viewdash) : <<include>>
  (retrieve) <. (filter) : <<include>>
  (move) .> (receive) : <<extends>>
  (save) .> (move) : <<extends>>
  (save) <. (retrieve) : <<extends>>
  (retrieve) .> (export) : <<include>>
}

Student -- viewdash
Student -- viewinst
Student -- remove
Student -- send

retrieve --> Database

move -- Car

@enduml
